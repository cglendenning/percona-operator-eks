{
  "name": "pxc-k8s-health-check",
  "version": "1.0.0",
  "private": true,
  "description": "Read-only Percona PXC Kubernetes namespace health report",
  "type": "commonjs",
  "main": "dist/cli.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli.js",
    "check": "npm run build && node dist/cli.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.5.4"
  }
}
